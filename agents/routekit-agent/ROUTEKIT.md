# RouteKit — document moved

The **canonical specification** for the `routekit-agent` crate (wire format, opcodes, vault tiers, events, build instructions, and roadmap) now lives in:

**[README.md](./README.md)**

The long-form orchestration sketch that previously lived in this file described aspirational routing (`agent_agent`, async frontier, compound tasks) that is **not** the current shipped WASM. Keeping a single README avoids drift between docs and `src/lib.rs`.

For historical content, see git history for `ROUTEKIT.md` before this redirect.
